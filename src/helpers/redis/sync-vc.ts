import { type Client } from "discord.js";

export const syncVCRedis = async (client: Client) => {
	console.log(client);
};

export const syncPartyRedisMongo = async () => {
	console.log("syncPartyRedisMongo");
};
